spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/engine?&useSSL=false&serverTimezone=UTC
    username: postgres
    password: newuserR-1
  jpa:
    properties:
      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQL131Dialect
        spring.jpa.hibernate.ddl-auto: update